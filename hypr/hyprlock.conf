background {
  monitor = 
  path = $HOME/.config/hypr/lockpaper.png

  blur_passes = 2 
  blur_size = 2
  noise = 0.05
}

label {
  monitor =
  text = Hi $USER :)
  color = rgba(200, 200, 200, 1.0)
  font_size = 35
  font_family = Cousine

  position = 0, 0
  halign = center
  valign = center
}

label {
  monitor = 
  text = $TIME
  color = rgba(200, 200, 200, 1.0)
  font_size = 50
  font_family = Cousine

  position = 0, 80
  halign = center
  valign = center
}
